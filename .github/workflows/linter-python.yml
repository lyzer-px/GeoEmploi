name: Ruff
on:
  push:
    paths: ['**.py']

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/ruff-action@v3
        with:
            args: check
            src: backend/app
